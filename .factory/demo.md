# Demo sandbox

Open `/demo` or `/?demo=1` to load three sample
bikes: Aster Road, Maple Cargo, and Pine Trail. Demo records use the separate
IndexedDB database `demo:bike-service-timeline`; real records use
`bike-service-timeline`. The persistent demo banner offers **Reset demo** and
**Start for real**. Reset discards only the demo database and reseeds it.
